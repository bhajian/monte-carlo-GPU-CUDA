apiVersion: v1
kind: Service
metadata:
  name: montecarlo-callback
  namespace: montecarlo
spec:
  selector:
    app: montecarlo-callback
  ports:
    - port: 8090
      targetPort: 8090
  type: ClusterIP
