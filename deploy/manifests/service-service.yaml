apiVersion: v1
kind: Service
metadata:
  name: montecarlo-service
  namespace: montecarlo
spec:
  selector:
    app: montecarlo-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
