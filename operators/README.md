# Operator Installation Materials

This folder contains helper scripts and values for installing GPU, NFD, MPI, and SR-IOV operators.

- `install.sh` installs Helm repos and applies charts
- `values-gpu-operator.yaml` is an example values file
