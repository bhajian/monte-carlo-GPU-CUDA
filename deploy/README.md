# Deployment Materials

This folder contains Kubernetes manifests and helper scripts for deploying the service and callback API.

- `manifests/` – namespace, deployments, services
- `templates/` – example worker Job / MPIJob manifests
- `deploy.sh` – apply manifests to the current kube context
